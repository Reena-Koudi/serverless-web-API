{
  "name": "lamba-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:watch": "echo \"Error: no test specified\" && exit 1",
    "test:e2e": "echo \"Error: no test specified\" && exit 1",
    "create": "claudia create --region eu-west-1 --api-module app",
    "deploy": "claudia update"
  },
  "author": "Reena Koudi",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.814.0",
    "claudia-api-builder": "^4.1.1",
    "superb": "^2.0.0",
    "uuid": "^3.3.0"
  }
}
